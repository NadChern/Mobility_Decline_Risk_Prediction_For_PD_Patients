# Part 1 paired judge evaluation

Prepared 360 of 360 expected blinded calls. All received reports are included, regardless of contract flags. Run this module with `--execute` after setting `OPENROUTER_API_KEY`. Raw responses and failures are retained.
